
{% extends "layout.html.twig" %}

{% block content %}

    <div class="container">

        <div class="card text-center">
            <div class="card-body">

                <h4 class="card-title">Attention</h4>
                <h5 class="card-text">{{ errorConnexion }}</h5>
                {% if isLogged %}
                <a href="/login" class="btn btn-primary" style="display: none" >Connectez-vous</a>
                {% else %}
                <a href="/login" class="btn btn-primary">Connectez-vous</a>
                {% endif %}
                <a href="{{ return }}" class="btn btn-primary">Retour au billet</a>

            </div>
        </div>

    </div>

{% endblock %}


